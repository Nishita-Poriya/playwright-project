# playwright-project
Project1 for playwright to explore

## Playwright commands

Run tests in the default headless mode:

```bash
npm test
```

Run tests in headed mode inside a virtual X server (works in this container):

```bash
npm run test:headed
```

Install Playwright browser binaries:

```bash
npm run install:browsers
```
